## 1、 安装RPM包
```text
rpm -i RPM包全路径名称   

rpm -ivh RPM包全路径名称：  i install v verbose h hash

rpm -qa : 查询所有安装的rpm软件

rpm -q 软件包名： 查询软件是否安装

rpm -qi 软件包名： 查询软件信息

rpm -ql 软件包名： 查询软件包中的文件

rpm -qf 文件全路径 查询文件所属的软件包

rpm -qp 包文件名： 查询包的信息对这个软件包的介绍

```
## 2、Samba安装